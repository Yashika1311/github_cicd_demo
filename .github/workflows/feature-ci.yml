name: Feature cicd development

on:
  push:
    branches:
      -feature**

jobs: ci
  ci:
    runs-on: ubuntu-last
    steps:
      - uses:actions/checkout@v3

      -name: runtest
       run: echo  "deploying on to the feature branch"  